type User {
  id: ID! @unique
  name: String!
  email: String!
}

type Device {
  id: ID! @unique
  deviceName: String!
  deviceId: String!
  createdAt: DateTime!
  updatedAt: DateTime!
  # user: User!
}
